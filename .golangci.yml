version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gocritic
    - misspell
    - gosec
    - bodyclose
    - nilerr
    - unconvert
  settings:
    revive:
      rules:
        - name: exported
          arguments:
            - disableStutteringCheck
        - name: package-comments
        - name: var-naming
        - name: context-as-argument
        - name: error-return
        - name: error-naming
    gocritic:
      disabled-checks:
        - ifElseChain
  exclusions:
    rules:
      # Generated golden fixtures and tests may relax some rules.
      - path: _test\.go
        linters:
          - gosec
          - errcheck

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/photon-hq/advanced-imessage-go
